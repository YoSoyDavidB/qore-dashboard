services:
  qore:
    build: .
    container_name: qore-dev
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - OPENCLAW_DIR=/openclaw
    volumes:
      - C:/Users/gusta/.openclaw:/openclaw:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
